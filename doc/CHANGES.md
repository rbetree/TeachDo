# TeachDo 变更记录（维护版）

> 更新：2026-03-20  
> 约定：本文件只记录会影响“对接/使用/迁移”的变更；实现细节与更细粒度的修改以 git 提交记录为准。

---

## 2026-03-20

- 文档口径统一：维护文档默认以“代码实现”为事实来源，历史/legacy 文档补充边界说明，避免口径漂移。
- API 命名对齐：新增推荐端点 `POST /tools/outline` 与 `POST /tools/ppt`；保留兼容别名 `POST /tools/aippt_outline_unified` 与 `POST /tools/aippt`（历史命名）。
- 前端对接更新：services 层与冒烟脚本默认改用新端点；旧端点仍可用，便于渐进迁移。
- 清理与对齐：移除未引用的旧组件与误导性文案，更新 `Slide JSON` 结构文档与相关路径引用。

